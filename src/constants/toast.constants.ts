/**
 * Toast stub for auth flows. Replace with sonner or similar when implemented.
 */
export const toastStub = {
    success: (_message: string) => {
        // Toast notification will be implemented with sonner library
        // For now, silently handle success
    },
};

export const NETWORK_TOAST_ID = "network-offline-toast";
