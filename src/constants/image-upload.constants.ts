/** Default max size in MB for image compression */
export const DEFAULT_COMPRESSION_MAX_SIZE_MB = 2;
/** Default max dimension (width or height) in px for compression */
export const DEFAULT_MAX_WIDTH_OR_HEIGHT = 1920;
/** Default initial quality for compression (0-1) */
export const DEFAULT_INITIAL_QUALITY = 0.8;
/** Default max size in MB for validation (before compression) */
export const DEFAULT_VALIDATION_MAX_SIZE_MB = 10;
/** Bytes per KB */
export const BYTES_PER_KB = 1024;
/** Bytes per MB */
export const BYTES_PER_MB = BYTES_PER_KB * BYTES_PER_KB;
