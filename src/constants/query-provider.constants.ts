/** One second in milliseconds */
export const SECONDS_TO_MS = 1000;
/** Default stale time for queries (seconds) */
export const DEFAULT_STALE_TIME_SECONDS = 30;
/** Default query retry count */
export const DEFAULT_QUERY_RETRY_COUNT = 1;
/** Default mutation retry count */
export const DEFAULT_MUTATION_RETRY_COUNT = 0;
