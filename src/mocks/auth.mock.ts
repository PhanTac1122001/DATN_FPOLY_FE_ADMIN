import type { LoginRequest } from "@/types/auth.types";

/** Seeded from extracted-portal/logic.js — password is UI stub only */
export const AUTH_LOGIN_MOCK: LoginRequest = {
    email: "minhanh.k18@rikkei.edu.vn",
    password: "password",
};
