# Mock data

UI / stub datasets for development. Seed from `LMS_AI_SPEC/extracted-portal/logic.js` when implementing basic-flow screens.

**Convention:** `<feature>.mock.ts` — e.g. `auth.mock.ts`, `home.mock.ts`.

Do not put mock arrays/objects in views or components. See `.cursor/rules/frontend.mdc` → Mock Data.
